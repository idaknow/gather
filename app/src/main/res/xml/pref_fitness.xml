<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- A 'parent' preference, which enables/disables child preferences (below)
         when checked/unchecked. -->
    <SwitchPreference
        android:defaultValue="false"
        android:key="fitness_all"
        android:title="Fitbit" />

    <SwitchPreference
        android:defaultValue="false"
        android:dependency="fitness_all"
        android:key="fitbit_activity"
        android:title="Activity" />

    <SwitchPreference
        android:defaultValue="false"
        android:dependency="fitness_all"
        android:key="fitbit_nutrition"
        android:title="Nutrition" />

    <SwitchPreference
        android:defaultValue="false"
        android:dependency="fitness_all"
        android:key="fitbit_heartrate"
        android:title="Heart Rate" />

    <SwitchPreference
        android:defaultValue="false"
        android:dependency="fitness_all"
        android:key="fitbit_weight"
        android:title="Weight" />

</PreferenceScreen>
